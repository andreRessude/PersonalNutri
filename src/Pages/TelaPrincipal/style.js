import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    tela:{
        width:"100%",
        height:"100%",
        alignItems:"center",
    },
    scrollView:{
        width:"100%",
        heigth:"100%"
    }
});

export default styles
